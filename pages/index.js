import Layout from '@/components/layout';
import PhotoGallery from '@/components/PhotoGallery';

const Home = () => {
  return (
    <Layout>
      <PhotoGallery />
    </Layout>
  );
};

export default Home;
